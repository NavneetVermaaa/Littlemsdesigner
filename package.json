{
  "name": "material-library",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "migrate:extract": "node scripts/extract-legacy.mjs",
    "migrate:legacy": "node scripts/build-legacy-scripts.mjs",
    "migrate:content": "node scripts/generate-page-content.mjs",
    "migrate": "npm run migrate:extract && npm run migrate:legacy && npm run migrate:content"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.6.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.3.5"
  }
}
